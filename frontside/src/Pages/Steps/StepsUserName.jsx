import React from 'react'

const StepsUserName = ({onNext}) => {
    return (
        <div>
            UserName
            <button onClick={onNext}>Next</button>
        </div>
    )
}

export default StepsUserName
